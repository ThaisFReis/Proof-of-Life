# Environment Configuration for Proof of Life Frontend

# Backend Mode: SIM (local simulation) or ONCHAIN (real Soroban RPC)
VITE_BACKEND_MODE=SIM

# Chain Configuration (required when VITE_BACKEND_MODE=ONCHAIN)
# VITE_RPC_URL=https://soroban-testnet.stellar.org
# VITE_CONTRACT_ID=C...
# VITE_NETWORK_PASSPHRASE=Test SDF Network ; September 2015

# For production/testnet deployment, uncomment and configure:
# VITE_BACKEND_MODE=ONCHAIN
# VITE_RPC_URL=https://soroban-testnet.stellar.org
# VITE_CONTRACT_ID=<deployed-contract-id>
# VITE_NETWORK_PASSPHRASE=Test SDF Network ; September 2015
#
# Secure ZK compatibility preflight (Proof of Life / ping verifier)
# Set these to the deployed verifier + exact circuit artifacts used by the prover.
# VITE_POL_EXPECTED_PING_VERIFIER=<deployed-ping-verifier-contract-id>
# VITE_POL_ZK_LAYOUT_VERSION=v3
# VITE_POL_PING_CIRCUIT_VERSION=v3
# VITE_POL_PING_VK_HASH=<32-byte-hex vk_hash from circuits/ping_distance/target/vk_hash>
